# Settings -> API in your Supabase project. Only needed to load flows saved
# through the builder via ?flow=<id>; the bundled demo flows (?flow=default,
# ?flow=eventLaunch) work without this.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

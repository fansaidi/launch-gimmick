# Supabase project's Postgres connection string (Settings -> Database ->
# Connection string -> URI). Use the "Direct connection" URI for running
# migrations (the session pooler doesn't support the prepared statements
# drizzle-kit needs).
DATABASE_URL=postgres://postgres:password@localhost:5432/postgres
